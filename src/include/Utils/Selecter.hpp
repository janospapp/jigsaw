#ifndef SELECTER_HPP
#define SELECTER_HPP

#include <vector>

class Selecter
{
public:
	Selecter();
	~Selecter();

    //void Select(GameObject *obj);
    //void Add(GameObject *obj);
    //void UnSelect(GameObject *obj);
	//void ClearSelect();
	//const std::vector<GameObject*>& GetSelected() const;
	
private:
	//std::vector<GameObject*> m_vecSelected;
};

#endif //SELECTER_HPP
